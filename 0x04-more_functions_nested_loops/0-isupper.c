#include "main.h"

/**
 * _isupper - uppercase letters.
 * @c : input char.
 *
 * Return: Returns 1 if c is uppercase and Returns 0 otherwise.
 */

int _isupper(int c)
{

	if (c >= 'A' && c <= 'A')
	{

		return (1);
	}
	else
		return (0);

}
